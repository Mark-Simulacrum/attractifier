/**
 *
 *
 * Spaces should not be added after the * in the above two lines.
 */

/**
 * Foobar
 */

/* Tests an HTTP proxy with a Headman eCAP adapter
 * configured to hide and reveal empty Content-Type headers. */

let a = // test
    bar;

let a = /* test
     * bar
     */
    foo;
